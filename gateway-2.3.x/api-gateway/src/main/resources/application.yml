server:
  port: 8765
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lowerCaseServiceId: true
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: replace-with-real-client-id
            client-secret: replace-with-real-client-secret
